# Neon Postgres (pooled connection for the app, direct for migrations)
DATABASE_URL=
DIRECT_URL=

# Upstash Redis (REST API)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Guards the /api/cron/sweep-expired endpoint
CRON_SECRET=
